apiVersion: v1
kind: Service
metadata:
  name: helloworld-service
  namespace: core
spec:
  selector:
    app: helloworld
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: LoadBalancer
